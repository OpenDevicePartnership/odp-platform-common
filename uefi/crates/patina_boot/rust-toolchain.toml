# Pinned Rust toolchain for patina_boot (UEFI targets).
#
# SPDX-License-Identifier: MIT
#

[toolchain]
channel = "nightly-2026-02-27"
targets = ["x86_64-unknown-uefi", "aarch64-unknown-uefi"]
components = ["rust-src", "clippy", "rustfmt"]
